source 'https://rubygems.org'

gem 'rack'

# Temporary work-around for the fact that rack_plus's dependancies aren't on
# rubygems.org yet.
gem 'rack_plus-app', github: 'rack-plus/rack_plus-app'
gem 'rack_plus-router', github: 'rack-plus/rack_plus-router'
gem 'rack_plus-controller', github: 'rack-plus/rack_plus-controller'

gem 'rack_plus', github: 'rack-plus/rack_plus'
